// import useCoverLetterContext from "../CoverLetterProvider/hook";

/**
 * Standard Sign-off footer
 * Pulled from profileInfo
 */
export default function CoverLetterFooter() {
  // const { profileInfo } = useCoverLetterContext();
  return (
    <div className="mt-8 text-sm text-slate-800">
      {/* TODO: Render sign-off line (e.g. "Sincerely,") and full name using profileInfo */}
    </div>
  );
}