// import useCoverLetterContext from "../CoverLetterProvider/hook";

/**
 * Meta block: Date, Target Company & Target Role
 * Pulled from coverLetter
 */
export default function CoverLetterMeta() {
  // const { coverLetter } = useCoverLetterContext();
  return (
    <div className="my-4 text-sm text-slate-700">
      {/* TODO: Implement company & role meta section
          Data available in coverLetter:
            date: string;
            companyName: string;
            roleName: string;
          Add inline editing or clean formatting matching the CV theme.
      */}
    </div>
  );
}