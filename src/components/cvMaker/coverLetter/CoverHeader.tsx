// import useCoverLetterContext from "../CoverLetterProvider/hook";

/**
 * Header: Applicant personal info (Name, Email, Phone, Links)
 * Pulled from profileInfo
 */
export default function CoverLetterHeader() {
//   const { profileInfo } = useCoverLetterContext();
  return (
    <div className="border-b pb-4 mb-4">
      {/* TODO: Implement the cover letter header with the applicant's personal info: Name, Email, Phone, Links */}
      {/* Here are the infos from profileInfo: 
            firstName: string;
            lastName: string;
            mail: string;
            phone: string;
            portfolio: string;
            linkedin: string;
            github: string;
            language: Language;
        You are not forced to use all of them, but you should at least use the firstName, lastName, mail and phone.
      */}
    </div>
  );
}